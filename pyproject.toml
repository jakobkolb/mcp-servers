[project]
name = "mcp-servers"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["servers/*"]

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.12"
strict = true
